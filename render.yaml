services:
  - type: web
    name: spiderfoot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 sf.py -l 0.0.0.0:10000
    autoDeploy: true
    branch: main
